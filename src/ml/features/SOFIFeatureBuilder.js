import { FinraFeatureBuilder } from './FinraFeatureBuilder.js';

export class SOFIFeatureBuilder extends FinraFeatureBuilder {
    constructor(ticker, repo, config) {
        super(ticker, repo, config);
    }
}
